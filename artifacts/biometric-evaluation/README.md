# Biometric evaluation data (local)

INTERNAL ONLY — not a production identity database.

Set `EVAL_BIOMETRIC_SECRET` and use `/internal/biometric-eval`.

Subject trees and exports are written here (or under `TRUSTID_EVAL_DATA_ROOT`).
Do not commit participant biometric artifacts.

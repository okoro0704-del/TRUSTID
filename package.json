{
  "name": "trustid",
  "private": true,
  "version": "1.0.0",
  "description": "TrustID V1 — Identity and Trust Infrastructure",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @trustid/api",
    "dev:web": "npm run dev -w @trustid/web",
    "dev:lifeos": "npm run dev -w @trustid/mock-lifeos",
    "build": "npm run build -w @trustid/shared && npm run build -w @trustid/api && npm run build -w @trustid/web && npm run build -w @trustid/mock-lifeos",
    "db:generate": "npm run db:generate -w @trustid/api",
    "db:push": "npm run db:push -w @trustid/api",
    "db:seed": "npm run db:seed -w @trustid/api",
    "setup": "npm install && npm run build -w @trustid/shared && npm run db:generate && npm run db:push && npm run db:seed",
    "test": "npm run test -w @trustid/api"
  },
  "engines": {
    "node": ">=20"
  }
}
